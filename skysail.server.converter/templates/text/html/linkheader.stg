linkheader(list) ::= <<
  <ul>
    $list:{ element | $handleSingleLinkheader(element,i)$}$
  </ul>
>>

handleSingleLinkheader(link,i) ::= <<

    <li>$i$. <a href='$link.uri$'>$link.title$</a> $link.rel$ $if(link.refId)$ [Ref#: $link.refId$]$endif$</li>
    
>>