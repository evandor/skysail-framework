renderEntity(user, source, model) ::= <<
  <table class="table">
    $model.formfields: {field | $renderRow(source.entity, field)$}$
  </table>
>>

renderRow(entity, field) ::= <<
  <tr><th>$field.name$</th><td>&nbsp;<td>$entity.(field.name)$</td><td>$handleDates(entity,field)$</td></tr>
>>

handleDates(entity, field) ::= <<
    $if(field.dateType)$
    <script>document.write(moment("$entity.(field.name)$").fromNow());</script>
    $endif$
>>